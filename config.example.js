// Copy this file to config.js and fill in your Apps Script URL.
// config.js is listed in .gitignore so it is never committed.
//
// How to get the URL:
//   1. Open your Google Sheet → Extensions → Apps Script
//   2. Paste google-apps-script/Code.gs, then Deploy → New deployment
//      (Execute as: Me, Who has access: Anyone)
//   3. Copy the Web App URL and paste it below.
//
// For GitHub Pages: add SHEET_URL as a repository secret instead of editing this file.
// Settings → Secrets and variables → Actions → New repository secret → Name: SHEET_URL

window.BTB_SHEET_URL = 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec';
